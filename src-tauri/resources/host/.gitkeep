# Placeholder so the `bundle.resources` path exists in a plain CI checkout:
# tauri-build hard-fails when a declared resource directory is missing. The
# real bundled runtime is generated by scripts/prepare-host.mjs (release
# workflow) and is gitignored.
